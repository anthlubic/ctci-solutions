const SingleLinkedList = require('./SingleLinkedList');

describe('utils: SingleLinkedList', () => {
	// it('appends nodes', () => {});
	// it('deletes nodes by index', () => {});
	// it('finds nodes by index', () => {});
});
