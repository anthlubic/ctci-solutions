const DoubleLinkedList = require('./DoubleLinkedList');

describe('utils - DoubleLinkedList', () => {
	// it('appends nodes', () => {});
	// it('deletes nodes by index', () => {});
	// it('finds nodes by index', () => {});
});
